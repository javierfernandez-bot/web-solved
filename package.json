{
  "name": "solved-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sitio estático de Solved (trysolved.com) + generador del blog headless desde WordPress.",
  "scripts": {
    "build:blog": "node scripts/build-blog.mjs",
    "build:redirects": "node scripts/build-redirects.mjs",
    "build:enlaces": "node scripts/build-enlaces.mjs",
    "build:sitemap": "node scripts/build-sitemap.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "wawoff2": "^2.0.1"
  }
}
